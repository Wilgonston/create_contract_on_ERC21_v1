// Update 13
import { getContractAddress } from "ethers";

async function zylsakdhfuFunc() {
  const proxyAddr = await upgrades.admin.getProxyAdmin(contract);
  console.log("Proxy on Base: ", proxyAddr);
}
// Update 39
import { getContractAddress } from "ethers";

async function ghktiprifeFunc() {
  const proxyAddr = await upgrades.admin.getProxyAdmin(contract);
  console.log("Proxy on Base: ", proxyAddr);
}
